import React from 'react';

const NotLogin = (props) => {
    return (
        <section className='not-login'>
            <p className='not-login-hint'>请使用oa账号登录</p>
        </section>
    );
};

export default NotLogin;